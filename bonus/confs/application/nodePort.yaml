apiVersion: v1
kind: Service
metadata:
  name: app-will-port
spec:
  type: NodePort
  selector:
    app: app-will
  ports:
    - port: 8080
      name: http
      targetPort: 8080
      nodePort: 30011